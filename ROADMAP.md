# LinkLake 路线图

[English](ROADMAP.en.md) | 中文

本文描述 `v1.0.0` 之后的产品方向。项目不再单独发布 `v1.0.1`；原定的体验、可靠性、协议扩展和高可用工作统一纳入 `v1.1.0`。版本范围是计划而不是发布日期承诺；实际状态、拆分任务与验收证据以 GitHub Milestone、Issue 和最终门禁为准。

## v1.1.0：完整功能版本

目标：在一个统一版本中补齐管理、更新、协议、多云、高可用和平台交付能力。开发阶段不发布中间版本；全部功能冻结后再集中执行完整测试、修复、灰度和正式发布。

### 管理界面

- 将 Web UI 的大型单文件拆分为独立页面、组件和状态模块，补齐响应式图表与安全更新入口。
- 补齐 Flutter Manager 的多材质主题、历史趋势、客户端状态图、完整用户与会话管理以及当前身份展示。
- 统一 Web UI 与 Manager 的权限、错误、更新、Fleet、健康和高可用状态语义。

### 安全远程更新

- 持久化服务端、客户端和 Manager 更新任务，提供检查、下载、调度、状态、失败恢复与回滚。
- 客户端必须主动启用并领取远程更新任务；服务端不得借更新通道执行任意命令。
- 对高风险操作强制交互式管理员权限、精确确认、CSRF/Origin 防护、并发锁、审计与失败关闭。

### 协议与目标池

- 完善 HTTP 正向代理连接复用和多请求 keep-alive。
- 实现 HTTP/1.1 `h2c Upgrade` 与 gRPC 本地目标 TLS/ALPN。
- 实现 SOCKS5 `BIND` 和 UDP `FRAG`，并加入端口、超时、顺序、内存预算与滥用防护。
- 对转发目标执行真实应用层健康探针；不健康目标自动摘除和恢复，加权选择只使用健康集合。

### 多云与 Fleet

- 区分节点在线、控制通道可用和真实业务可达性。
- 展示 Fleet generation、来源、ownership、同步进度和冲突详情，并支持预览与人工解决。
- 完善 Cloudflare DNS 故障切换演练、冻结、冷却、恢复和多服务器更新协调。

### PostgreSQL 与高可用

- 引入 PostgreSQL 存储后端和显式迁移台账，同时保留 SQLite 单实例模式。
- 实现实例租约、Leader 选举、fencing token、公网端口所有权和安全故障接管。
- 保证证书续期、更新、告警和 Fleet 等后台任务在多实例环境中唯一执行并可恢复。
- 在 Web UI 与 Manager 中展示成员、租约、端口所有权、接管和异常状态。

### 平台、依赖与发行

- 完善 Windows、DEB、RPM 和 Helm 的安装、升级、配置保护、失败恢复与卸载语义。
- 将框架、数据库、密码学、配置解析、GitHub Actions 和 Docker 工具链升级拆成可审查批次，不直接合并混合重大升级。
- 所有功能冻结后统一执行 Rust、Flutter、浏览器、全协议、更新、HA、安装器和供应链测试；通过后先上海灰度，再升级新加坡并正式发布。

验收：所有公开能力具有一致的 API 与管理入口；更新和权限保持失败关闭；协议与高可用边界可审计；统一测试、灰度和发布门禁全部通过。

## 持续边界

- 长时间稳定性和弱网矩阵继续由独立 soak 工作流执行，不阻塞本轮功能开发与短时统一验收。
- macOS 保持源码与 CI 兼容，当前不承诺官方安装包、Developer ID、公证或自动更新。
- Windows 官方资产按个人开源策略保持无 Authenticode 签名；用户应从官方 Release 获取并验证 SHA-256、GitHub Attestation 与 LinkLake Ed25519 清单。
- 任何发布签名、数据库迁移、权限或网络策略更改都必须失败关闭，并保留可验证的恢复路径。
