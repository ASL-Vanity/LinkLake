# LinkLake 路线图

[English](ROADMAP.en.md) | 中文

本文描述 `v1.0.0` 之后的产品方向。版本范围是计划而不是发布日期承诺；实际状态、拆分任务与验收证据以 GitHub Milestone、Issue 和 CI 为准。

## v1.0.1：体验与维护性

目标：在不改变现有协议语义和配置兼容性的前提下，降低管理界面的维护成本并补齐桌面管理体验。

- 将 Web UI 的大型单文件拆分为独立页面、组件和状态模块，保持现有 RBAC 与浏览器测试语义。
- 使用容器尺寸观察优化宽屏、窄屏、抽屉和图表响应，并提供可发现的安全更新管理入口。
- 补齐 Flutter Manager 的多材质主题、历史趋势、客户端状态图、用户完整编辑、当前用户与角色展示。
- 建立 `main` 合并门禁、`v*` 标签不可变规则、版本 Milestone 与可执行 Issue。
- 审查自动依赖更新；重大依赖升级必须拆分、迁移并经过完整协议与供应链门禁，不批量盲合。

验收：现有协议 E2E、安全基线和三平台构建门禁全部通过；Web UI 与 Manager 的新增入口有自动化覆盖；发布、更新和回滚仍保持失败关闭。

## v1.1.0：可靠性与多云可见性

目标：让多目标与多服务器部署能够根据真实业务健康状态自动恢复，并让运维人员看清每次决策。

- 对转发目标执行真实应用层健康探针，支持阈值、摘除、恢复和防抖。
- 将加权目标池与健康状态联动，同时保留明确的失败与回退指标。
- 增强 Fleet、Cloudflare DNS 故障切换演练、同步冲突和来源身份的可视化。
- 建立 Windows、DEB、RPM 的首次安装、升级、失败回滚和配置保护 E2E。
- 统一 Web UI 与 Flutter Manager 的多云状态、更新状态和故障诊断语义。

验收：健康摘除与恢复可以被确定性复现；故障切换有审计记录和指标；安装器 E2E 覆盖成功、失败和回滚路径。

## v1.2.0：高可用控制平面与协议扩展

目标：在明确一致性边界后支持多实例控制平面，并逐项扩展现代后端协议。

- 引入 PostgreSQL 存储后端和显式迁移工具；SQLite 保留为单实例部署选项。
- 实现多实例租约、端口所有权、故障接管和防止双重监听的协调机制。
- 为 gRPC 本地目标增加 TLS/ALPN，并评估 HTTP/1.1 `h2c Upgrade` 的安全实现。
- 分别评估 SOCKS5 `BIND` 和 UDP `FRAG` 的真实需求、攻击面与互操作性；未通过评估前继续明确拒绝，而不是静默降级。

验收：多实例故障接管不会产生双写、双监听或越权接管；新协议能力具备明确边界、负面测试、审计和端到端验证。

## 持续边界

- 长时间稳定性和弱网矩阵继续由独立 soak 工作流执行，不作为短周期功能开发的替代品。
- macOS 保持源码与 CI 兼容，当前不承诺官方安装包、Developer ID、公证或自动更新。
- Windows 官方资产按个人开源策略保持无 Authenticode 签名；用户应从官方 Release 获取并验证 SHA-256、GitHub Attestation 与 LinkLake Ed25519 清单。
- 任何发布签名、数据库迁移、权限或网络策略更改都必须失败关闭，并保留可验证的恢复路径。
