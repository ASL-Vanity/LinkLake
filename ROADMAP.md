# LinkLake 路线图

[English](ROADMAP.en.md) | 中文

本文区分 `v1.1.0` 已在仓库实现的能力与后续持续改进方向。原定的体验、可靠性、协议扩展和高可用工作合并到 `v1.1.0`，不再单独安排 `v1.0.1`。后续方向不代表发布日期承诺；具体变更与发行资产以 [更新日志](CHANGELOG.md)和对应 Release 为准。

## v1.1.0 当前状态

- PostgreSQL 共享身份、会话、八类策略、Fleet、证书/ACME、流量及运维状态；保留 SQLite 单机模式，共享读取失败不回退本机业务库。
- 实例租约、Leader fencing、共享端口所有权、Fleet 原子协调及持久流量 spool 已接入；各副本保留独立持久目录。
- Web UI 与 Flutter Manager 已提供 ACME 挑战配置、就绪状态、HA/Fleet 管理及角色权限；协议能力包含 gRPC 后端 TLS、SOCKS5 BIND/受限 FRAG 和动态出口保护。
- 显式 PostgreSQL 初始化、SQLite 迁移、回滚资格检查与停机材料密钥轮换已提供命令入口；升级流程区分 SQLite 单机和 PostgreSQL 集群。
- Helm 共享存储与独立卷配置、安装/升级保护、[使用说明](docs/user-guide.zh-CN.md)及中英文 README 已完成本轮更新。

当前后续事项为版本提交、正式构建与签名、发布资产及发行说明。本轮材料准备完毕后直接进入发布，不再追加新一轮全量回归或绑定特定地域的灰度顺序。生产服务器升级单独安排；仓库实现和版本发布都不代表生产节点已经升级。

## 持续演进方向

以下事项用于发布后的持续改进，不作为本轮新增前置流程。

### 管理界面

- 持续整理 Web UI 的页面、组件和状态边界，优化响应式图表与安全更新入口。
- 继续打磨 Flutter Manager 的多材质主题、历史趋势、客户端状态图、用户与会话管理以及当前身份展示。
- 持续对齐 Web UI 与 Manager 的权限、错误、更新、Fleet、健康和高可用状态语义。

### 安全远程更新

- 完善服务端、客户端和 Manager 持久更新任务的检查、下载、调度、状态及恢复体验，保持数据库兼容性与回滚边界明确。
- 客户端必须主动启用并领取远程更新任务；服务端不得借更新通道执行任意命令。
- 对高风险操作强制交互式管理员权限、精确确认、CSRF/Origin 防护、并发锁、审计与失败关闭。

### 协议与目标池

- 完善 HTTP 正向代理连接复用和多请求 keep-alive。
- 完善 HTTP/1.1 `h2c Upgrade` 与 gRPC 本地目标 TLS/ALPN 的复杂后端兼容性。
- 持续完善 SOCKS5 `BIND` 和 UDP `FRAG` 的端口、超时、顺序、内存预算与滥用防护。
- 优化真实应用层健康探针在弱网和慢目标下的行为；保持不健康目标摘除与恢复、加权选择只使用健康集合。

### 多云与 Fleet

- 区分节点在线、控制通道可用和真实业务可达性。
- 改进 Fleet generation、来源、ownership、同步进度和冲突详情的展示，以及预览与人工解决流程。
- 完善 Cloudflare DNS 故障切换演练、冻结、冷却、恢复和多服务器更新协调。

### PostgreSQL 与高可用

- 演进 PostgreSQL schema 兼容性、显式迁移台账与灾后恢复操作，同时保留 SQLite 单实例模式。
- 完善实例租约、Leader 选举、fencing token、公网端口所有权和故障接管在复杂故障下的行为。
- 完善证书续期、更新、告警和 Fleet 后台任务的租约、幂等、恢复及外部副作用复核；不把数据库 fencing 表述为跨外部服务的事务保证。
- 优化 Web UI 与 Manager 中成员、租约、端口所有权、接管和异常状态的呈现。

### 平台、依赖与发行

- 完善 Windows、DEB、RPM 和 Helm 的安装、升级、配置保护、失败恢复与卸载语义。
- 将框架、数据库、密码学、配置解析、GitHub Actions 和 Docker 工具链升级拆成可审查批次，不直接合并混合重大升级。
- 后续发行继续维护正式构建、资产身份、校验和与签名流程；生产部署按各环境的数据库状态、恢复方案和维护窗口单独安排。

持续质量目标：公开能力具有一致的 API 与管理入口；更新和权限保持失败关闭；协议、账务、数据库恢复与高可用边界可审计。该目标不表示所有环境或生产灰度均已完成验收。

## 持续边界

- 长时间稳定性和弱网矩阵保留为独立 soak 工作流，不新增为本轮发布前置项。
- macOS 保持源码与 CI 兼容，当前不承诺官方安装包、Developer ID、公证或自动更新。
- Windows 官方资产按个人开源策略保持无 Authenticode 签名；用户应从官方 Release 获取并验证 SHA-256、GitHub Attestation 与 LinkLake Ed25519 清单。
- 任何发布签名、数据库迁移、权限或网络策略更改都必须失败关闭，并保留可验证的恢复路径。
