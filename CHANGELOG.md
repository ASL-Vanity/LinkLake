# LinkLake 更新日志

本项目采用语义化版本号。候选版本用于完整验收，不代表已对外发布。

## 0.5.0-rc.1 - 2026-08-01

### 管理界面与权限

- Web UI 扩展为约 2200px 宽屏工作区，统一顶部服务状态、刷新、外观、语言和账户入口，并在宽屏使用五列 KPI 与三列详细指标。
- 服务健康度改为在线、离线、停用和总计；协议活动使用横向图表，故障图使用警告/错误语义色。
- 四套配色均覆盖浅色和深色下的页面背景、卡片、输入框、边框、阴影、图表和状态色；跟随系统模式保留用户选择的配色。
- 新增管理员、运维人员和审计人员角色，以及用户创建、编辑、启停、删除、密码重置、强制改密和会话撤销界面/API。
- 后端强制执行 RBAC：审计人员只读，运维人员不能管理用户与会话；当前用户和最后一个启用的管理员受到保护。

### 30 天历史指标

- 流量范围统一为 `1h、12h、1d、7d、30d`，默认 `1h`。
- 12 小时内保留 5 秒精度样本，长期按分钟归档并保留 30 天；API 根据范围降采样到最多约 300 点。
- 数据库架构版本升级为 2，升级前沿用自动一致性备份流程。

### 多云入口

- 客户端配置版本升级为 2，新增多个命名 `[[servers]]` 身份；同一客户端进程可同时连接多台 LinkLake 服务端。
- 每个云入口独立同步服务端托管配置、保存 `managed.<server-name>.toml` 并运行转发任务；单个入口断线不影响其他入口。
- `local` 与 `report_only` 策略可复制到全部入口；`server_managed` 模式可在各云 Web UI 中分别把同一目标服务发布为不同公网端口。
- 多云 Secret 访问端使用 `server` 字段明确选择入口，避免本地监听和访问密钥被错误复制。

### 可配置公网端口

- 新增 TCP/UDP 独立的单端口、多区间允许策略和保留端口策略，范围覆盖 `1-65535`，默认继续使用 `32000-32999`。
- 自动保留管理、控制、HTTP/HTTPS、TLS SNI 和 UDP relay 的实际监听端口；TCP 端口 22 默认保留。
- 数据库存量策略在启动时校验；新策略若排除现有端口，服务端明确拒绝启动，避免策略静默离线。
- 新增 `GET /api/v1/public-port-policy`，Web UI 动态显示允许范围与保留端口，并移除固定端口输入限制。
- 客户端托管配置与本地端口组解除固定范围假设，由对应服务端策略执行最终授权。

### 验证

- 新增端口区间规范化、完整端口空间、保留端口、数据库策略执行和多云配置隔离测试。
- Web UI 冒烟测试新增多用户 CRUD、角色权限、完整主题、五档历史范围、2048px 宽屏和 390px 移动端覆盖。

## 0.4.0-rc.2 - 2026-07-31

### Web UI

- 使用双岸 Logo、favicon 和统一品牌登录页，登录后显示管理员用户名、角色和会话到期时间。
- 总览改为 KPI、流量趋势和图表化运行指标；活动日志采用紧凑列表、筛选和展开详情。
- TCP、UDP、端口组、HTTP/HTTPS、TLS SNI、Secret、SOCKS5 与 HTTP Proxy 使用独立页面和统一编辑抽屉。
- 增加跟随系统、浅色、深色模式以及湖蓝、海洋、翡翠、紫罗兰四套配色。
- 增加路由级按需请求、刷新单飞、请求取消和会话代次隔离；完成 390px 移动端无横向溢出验证。

### 安全与指标

- 管理会话改用 SHA-256 摘要与常量时间比较，旧 Argon2 会话明确撤销；管理员改密后只保留当前会话。
- 登录增加单账户指数退避、全局失败间隔、密码哈希并发限制和未知用户名 dummy Argon2 校验。
- 新增 `/api/v1/auth/me` 与 24 小时历史指标接口，支持范围、协议和最多 300 点下采样。
- 修正 TCP、UDP 与 Secret 历史错误统计的重复计数和协议归属。

### 开源与发布

- 确认全项目采用 Apache License 2.0，并增加第三方许可证、商标与贡献政策。
- 发布包增加 `THIRD_PARTY_NOTICES.md`、完整 Rust 许可证文本和 `TRADEMARKS.md`。
- CI 使用 cargo-about 校验锁定依赖的许可证清单；RC 标签自动创建 GitHub Pre-release。

### 验收

- Rust 工作区 170 项测试、格式检查和 Clippy 通过。
- 真实 Chrome Web UI 冒烟测试完成八类策略编辑、主题持久化、账户恢复、Hash 路由、活动展开和移动端溢出验证。
- 新版 Linux x86_64 服务端已部署至新加坡测试服务器，管理 API 和八类协议列表通过公网验证。

## 0.4.0-rc.1 - 2026-07-31

### 新增

- TCP、UDP、连续或离散端口组、HTTP/HTTPS、TLS SNI 原样透传、Secret、SOCKS5 TCP/UDP 与 HTTP 正向代理/CONNECT。
- Secret P2P 支持 Noise `NNpsk0` + ChaCha20-Poly1305 TCP 加密、Iroh QUIC/UDP 打洞和受控服务端中继回退。
- STUN/QAD 公网地址、NAT 映射行为、路由器端口映射和自托管 Iroh 会合服务信息上报。
- 多候选并发竞速，单次票据只发送到胜出连接；Iroh relay-only 路径不承载业务数据。
- 双语 Web UI、账号密码登录、首次登录强制改密、服务端托管配置与审计/指标。
- Windows、Linux、macOS 服务定义和可校验发布包；Linux Pebble HTTPS/ACME 自动化。
- Flutter Manager 的 Windows、Linux、macOS CI、Release 构建和独立可校验发布包。
- 项目许可证确定为 Apache License 2.0，并将 `LICENSE` 与 `NOTICE` 纳入全部发布包。

### 可靠性与安全

- 一次性访问密钥和代理密码仅返回一次，数据库保存哈希。
- P2P 票据短期、单次消费、防重放；每个会话使用独立 PSK，双方定期 rekey。
- SQLite 在线备份、完整性校验恢复、启动迁移前自动备份和向前版本拒绝。
- 并发、分片弱网、吞吐、限速、进程崩溃、客户端重连、服务端重启和 soak 测试矩阵。
- 每周及手动触发的 Windows 长稳矩阵工作流，失败时保留可下载报告。
- 普通 HTTP 正向代理在完整响应返回后再关闭 TLS 数据通道，避免 Windows 客户端提前 EOF。
- TCP 类数据通道把缺失 TLS `close_notify` 的底层 EOF 作为半关闭处理，保留已传输字节且不再制造失败指标。
- 公网验收逐项原子保存结果，并为允许丢包的 UDP 可达性检查提供有限重试。
- 扩大受全局字节预算约束的每会话 UDP 接收窗口，避免 QUIC 或 Windows 调度产生的正常小突发导致间歇性丢包。
- Secret 长稳验收在验证 P2P 自动回退后显式切换为中继模式，并按 TCP 流语义读取完整响应，避免旧候选拨号等待造成误判。

### 验收

- Windows 客户端、新加坡服务端与上海公网验收机完成 16/16 数据通道实测。
- Rust 工作区测试、Clippy、Windows/Linux 发布包校验及 Flutter 管理端分析、测试和 Windows Release 构建通过。

### 升级说明

- 服务端首次打开旧数据库时，会在数据目录创建 `linklake.sqlite3.pre-migration-v<版本>-<时间>` 后再提交架构版本。
- 客户端可运行 `linklake-client migrate-config --input old.toml --output new.toml` 生成并验证当前格式；不会覆盖原文件。
